export interface Category {
    id: string
    name: string
    icon: string
    color: string
    createdAt: string
}

export interface CreateCategoryInput{
    name: string
    icon: string
    color: string
}

